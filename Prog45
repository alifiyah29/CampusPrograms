#include<stdio.h>
#include<stdlib.h>
main(){
    int n,sum=0;
    scanf("%d",&n);
    int *ptr = (int *)malloc(n*sizeof(int));
    for(int i=0;i<n;i++)
        scanf("%d",&ptr[i]);
    for(int i=0;i<n;i++)
        sum = sum+ptr[i];   
    printf("%d",sum);    
}
